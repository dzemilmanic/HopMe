# ============================================
# .env.example - HopMe Backend Configuration
# ============================================
# Copy this file to .env and populate with the correct values

# Server Configuration
PORT=5000
NODE_ENV=development

# PostgreSQL Database (Hosted)
DB_HOST=your-postgres-host.com
DB_PORT=5432
DB_NAME=your_database_name
DB_USER=your_username
DB_PASSWORD=your_strong_password_here
DB_SSL=true

# JWT Configuration
JWT_SECRET=generate_a_very_long_random_string_here_minimum_32_characters
JWT_EXPIRE=7d

# Email Configuration (Resend - https://resend.com)
# Sign up at resend.com and get your API key
RESEND_API_KEY=re_xxxxxxxx
EMAIL_FROM=onboarding@resend.dev

# Frontend URL
FRONTEND_URL=http://localhost:3000

# Azure Blob Storage
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=your_account_name;AccountKey=your_account_key;EndpointSuffix=core.windows.net
AZURE_STORAGE_CONTAINER_NAME=vehicle-images

# ============================================
# NOTES:
# ============================================
# 
# 1. JWT_SECRET: Generate a strong random string (min 32 characters)
#    Online generator: https://randomkeygen.com/
#    Or in terminal: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# 2. EMAIL_PASS: For Gmail use App Password, not your regular password
#    How to do it: https://support.google.com/accounts/answer/185833
#
# 3. AZURE_STORAGE: Get from Azure Portal → Storage Account → Access Keys
#
# 4. DB_SSL: Set to true for hosted databases (Railway, Supabase, etc.)
#
# 5. FRONTEND_URL: URL of your React/SwiftUI application for email links
#